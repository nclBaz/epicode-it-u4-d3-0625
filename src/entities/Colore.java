package entities;

public enum Colore {
	ROSSO, VERDE, BLU // Serve per limitare un attributo a una lista finita di valori
	// e non dare la possibilità come per le String di far inserire qualsiasi testo
}
